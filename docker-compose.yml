services:
  vault:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - vault-data:/app/data
    environment:
      - SECURE_COOKIES=false
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true

volumes:
  vault-data: